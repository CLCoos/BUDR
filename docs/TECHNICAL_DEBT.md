# Technical debt

- **ParkLoginNotice.tsx** og `park_daily_checkin` compat view skal omdøbes i en fremtidig sprint (intern beboer-login, ikke marketing).
- **GitHub CLI:** `gh` er installeret via Homebrew (`brew install gh`). Kør `gh auth login` én gang (browser-flow) så PR og deploy-preview kan hentes fra terminal uden manuelle klik.
